/**
 * Modern Child-Friendly Typography System
 * Adventures of Trishu Mini-Game Suite
 * Uses Fredoka with modern rounded fallbacks
 * Decoupled & strictly under 500 lines
 */

export const FONT_FAMILY = '"Fredoka", "Quicksand", "Arial Rounded MT Bold", -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif';

export const FONTS = {
  TITLE_HERO: `800 24px ${FONT_FAMILY}`,
  TITLE_CARD: `700 16px ${FONT_FAMILY}`,
  TITLE_CARD_LANDSCAPE: `700 14px ${FONT_FAMILY}`,
  SUBTITLE: `500 12px ${FONT_FAMILY}`,
  SUBTITLE_SMALL: `500 10.5px ${FONT_FAMILY}`,
  BADGE: `700 10.5px ${FONT_FAMILY}`,
  SCORE_BADGE: `800 11px ${FONT_FAMILY}`,
  HUD_SCORE: `800 18px ${FONT_FAMILY}`,
  BUTTON: `700 15px ${FONT_FAMILY}`
};
